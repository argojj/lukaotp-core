# LukaOTP Core privacy note

This document describes only the Core-only library. It does not collect,
transmit, retain, sell, or share user data. It has no network client or account
service. Applications that embed the library determine where encrypted data and
backup files are stored and must provide their own user-facing privacy notice.
